version: '3.8'
services:
  app:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PUBLIC_WIX_CLIENT_ID=${PUBLIC_WIX_CLIENT_ID}
